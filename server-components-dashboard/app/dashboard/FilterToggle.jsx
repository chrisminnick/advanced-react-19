'use client';

// Empty placeholder. Lab 5 Part D has you fill this in.
//
// The point of this component being 'use client':
//   - It owns a piece of UI state (a checkbox value)
//   - It still receives Server Components as children — students wrap
//     <ActivityFeed /> in this without importing it directly
//
// Lab 5 walks through the composition pattern explicitly.

export default function FilterToggle({ children }) {
  return <>{children}</>;
}
