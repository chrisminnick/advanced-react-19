/** @type {import('next').NextConfig} */
const nextConfig = {
  // Nothing custom — defaults are fine for the demo. Listed so future
  // tweaks (image domains, rewrites, etc.) have a place to land.
};

export default nextConfig;
